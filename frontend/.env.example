# =============================================================================
# VeriTrust frontend — environment template (SAFE TO COMMIT)
# Copy to frontend/.env locally:
#   cp frontend/.env.example frontend/.env   (Linux/macOS)
#   copy frontend\.env.example frontend\.env (Windows)
#
# NEVER commit frontend/.env — it is listed in .gitignore.
# =============================================================================

VITE_API_URL=http://localhost:8000
